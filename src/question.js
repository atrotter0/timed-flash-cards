export class Question {
  constructor(level, points, category, text, answer) {
    this.level = level;
    this.points = points;
    this.category = category;
    this.text = text;
    this.answer = answer;
  }
}
